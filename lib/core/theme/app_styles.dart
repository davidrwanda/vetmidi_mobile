import 'package:flutter/material.dart';

abstract class AppStyles {
  static const radius5 = BorderRadius.all(Radius.circular(5));
  static const radius15 = BorderRadius.all(Radius.circular(15));
  static const radius25 = BorderRadius.all(Radius.circular(25));

  static const spacing10 = EdgeInsets.all(10);
  static const spacing20 = EdgeInsets.all(20);
}
